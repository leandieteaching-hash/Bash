import {adminDb,errorResponse,requirePermission,response,ApiError} from '@/lib/spread-manager-server';
export async function POST(request:Request){try{const actor=await requirePermission('asset.upload');const body=await request.json();if(!body.bookId||!body.title||!body.storagePath)throw new ApiError(400,'INVALID_ASSET','bookId, title and storagePath are required.');const {data,error}=await adminDb().rpc('workflow_finalize_asset_upload',{p_publication_id:body.bookId,p_spread_id:body.spreadId||null,p_asset_id:body.assetId||null,p_title:body.title,p_asset_type:body.assetType,p_storage_path:body.storagePath,p_filename:body.filename,p_content_type:body.contentType,p_size_bytes:body.sizeBytes,p_tags:body.tags||[],p_actor_profile_id:actor.profileId});if(error)throw error;return response(data,201)}catch(error){return errorResponse(error)}}
