import {NextResponse} from 'next/server';import {z} from 'zod';import {readSession} from '@/lib/platform/identity';import {createCycle} from '@/features/reviews/review-service';
const schema=z.object({title:z.string().trim().min(1).max(160),dueAt:z.string().datetime().optional()});
export async function POST(request:Request,{params}:{params:Promise<{spreadId:string}>}){const requestId=request.headers.get('x-request-id')??crypto.randomUUID();try{const identity=await readSession(request);if(!identity?.organisationId)return NextResponse.json({error:{code:'UNAUTHENTICATED'},requestId},{status:401});const {spreadId}=await params;return NextResponse.json({data:await createCycle(identity,spreadId,schema.parse(await request.json()),requestId),requestId},{status:201})}catch(error){const forbidden=String(error).includes('FORBIDDEN');return NextResponse.json({error:{code:forbidden?'FORBIDDEN':'REVIEW_CREATE_FAILED'},requestId},{status:forbidden?403:400})}}
