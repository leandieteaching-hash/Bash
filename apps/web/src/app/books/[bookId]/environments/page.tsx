import {PageHeader} from '@/components/layout/Page';export default function Page(){return <><PageHeader title="Environments" description="Locations, backgrounds, references and spread usage." action={<button className="button buttonPrimary">Add Environment</button>}/><div className="entityGrid">{[1,2,3,4,5,6].map(i=><div className="card" key={i}><span className="badge">Environments</span><h3>Environments item {i}</h3><p className="muted">Integration-ready placeholder backed by the shared feature contract.</p></div>)}</div></>}
