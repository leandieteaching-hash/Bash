import {PageHeader} from '@/components/layout/Page';export default function Page(){return <><PageHeader title="Characters" description="Character reference sheets, consistency notes and appearances." action={<button className="button buttonPrimary">Add Character</button>}/><div className="entityGrid">{[1,2,3,4,5,6].map(i=><div className="card" key={i}><span className="badge">Characters</span><h3>Characters item {i}</h3><p className="muted">Integration-ready placeholder backed by the shared feature contract.</p></div>)}</div></>}
