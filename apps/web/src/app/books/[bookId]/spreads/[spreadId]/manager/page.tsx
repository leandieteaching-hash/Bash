import {Breadcrumbs} from '@/components/layout/Page';import {SpreadManager} from '@/features/spread-manager/SpreadManager';export default async function Manager({params}:{params:Promise<{bookId:string;spreadId:string}>}){const{bookId,spreadId}=await params;return <><Breadcrumbs items={[{label:'Books',href:'/books'},{label:'Meet Mia',href:`/books/${bookId}/overview`},{label:'Spreads',href:`/books/${bookId}/spreads`},{label:spreadId},{label:'Manager'}]}/><SpreadManager spreadId={spreadId}/></>}
