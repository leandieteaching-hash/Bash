import Link from 'next/link';import {Breadcrumbs,PageHeader} from '@/components/layout/Page';import {books} from '@/lib/demo-data';
export default function Books(){return <><Breadcrumbs items={[{label:'Dashboard',href:'/dashboard'},{label:'Books'}]}/><PageHeader title="Books" description="Browse and manage publications." action={<button className="button buttonPrimary">New book</button>}/><div className="bookGrid">{books.map(book=><Link className="bookCard" key={book.id} href={`/books/${book.id}/overview`}><div className="cover">📚</div><div className="cardBody"><h3>{book.title}</h3><p className="muted">{book.subtitle}</p><div className="progress"><span style={{width:`${book.progress}%`}}/></div><p><strong>{book.progress}%</strong> complete · {book.openTasks} tasks</p></div></Link>)}</div></>}
