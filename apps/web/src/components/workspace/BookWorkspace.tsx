'use client';
import Link from 'next/link';import {usePathname} from 'next/navigation';import {demoBook} from '@/lib/demo-data';import {sectionLabels,workspaceSections} from '@/lib/routes';
export function BookWorkspaceHeader({bookId}:{bookId:string}){const path=usePathname();return <><section className="workspaceHeader"><div className="workspaceCover">📘</div><div className="workspaceMeta"><h1>{demoBook.title}</h1><div className="muted">{demoBook.subtitle}</div><div className="progress" style={{marginTop:12,maxWidth:420}}><span style={{width:`${demoBook.progress}%`}}/></div></div><div className="workspaceStats"><strong>{demoBook.progress}%</strong><div className="muted">Complete</div></div></section><nav className="workspaceTabs" aria-label="Book workspace">{workspaceSections.map(section=>{const href=`/books/${bookId}/${section}`;return <Link key={section} href={href} data-active={path===href||path.startsWith(href+'/')}>{sectionLabels[section]}</Link>})}</nav></>}
