'use client';
import {useState, type FormEvent} from 'react';
export function ForgotPasswordForm(){const[pending,setPending]=useState(false);const[done,setDone]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);const form=new FormData(e.currentTarget);await fetch('/api/v1/auth/password/forgot',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:form.get('email')})});setPending(false);setDone(true)}if(done)return <div className="authNotice success" role="status">Check your inbox. If an account exists, we sent password reset instructions.</div>;return <form className="authForm" onSubmit={submit}><label>Email address<input name="email" type="email" autoComplete="email" required/></label><button className="button buttonPrimary" disabled={pending}>{pending?'Sending…':'Send reset link'}</button></form>}
