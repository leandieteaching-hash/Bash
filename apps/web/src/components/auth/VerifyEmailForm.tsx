'use client';
import {useState,type FormEvent} from 'react';
export function VerifyEmailForm(){const[pending,setPending]=useState(false);const[done,setDone]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);const form=new FormData(e.currentTarget);await fetch('/api/v1/auth/email/verify',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:form.get('email')})});setPending(false);setDone(true)}return done?<div className="authNotice success">If the address is eligible, a verification email has been sent.</div>:<form className="authForm" onSubmit={submit}><label>Email address<input name="email" type="email" required/></label><button className="button buttonPrimary" disabled={pending}>{pending?'Sending…':'Resend verification email'}</button></form>}
