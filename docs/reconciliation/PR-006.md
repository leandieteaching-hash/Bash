# PR-006 reconciliation — Application Shell and Design System

The application shell and design-system foundation predated the linear feature history. This reconciliation record confirms the retained implementation on `main`:

- authenticated `AppShell` and page layout components;
- central CSS design tokens and design-system reference page;
- loading, error and not-found boundaries;
- tenant switcher and authorization-aware administrative navigation;
- application-shell structural release test.

The retained implementation is represented by the files under `apps/web/src/components/layout`, `apps/web/src/styles`, `apps/web/src/app/design-system`, and `apps/web/tests/application-shell.sh`.
